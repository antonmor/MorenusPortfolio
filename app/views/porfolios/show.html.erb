<%= image_tag @portfolio_items.thumb_image if !@portfolio_items.thumb_image.nil? %>

<h1><%= @portfolio_items.title %></h1>

<em> <%= @portfolio_items.subtitle %> </em>

<em><%= @portfolio_items.body %></em>

<%= link_to 'Volver', :action => :index   %>
<%= link_to 'Edit', edit_porfolio_path%>

<h2>Technologies used: </h2>
<% @portfolio_items.technologies.each do |t| %>
  <ul>
    <li>
      <%= t.name %>
    </li>
  </ul>
<% end %>